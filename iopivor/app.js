(()=>{const RULES=window.IOPIVOR_RULES||[];const q=document.getElementById('q'),mic=document.getElementById('mic'),hint=document.getElementById('hint'),results=document.getElementById('results'),infoBtn=document.getElementById('infoBtn'),info=document.getElementById('info');infoBtn.addEventListener('click',()=>info.classList.toggle('hidden'));const norm=s=>String(s||'').toLowerCase().replace(/ё/g,'е').replace(/[«»“”„"'()\[\]{}:;,.!?/\\\-]+/g,' ').replace(/\s+/g,' ').trim();const stop=new Set('и или а но что как какой какая какое какие кто где когда для при на в во по из от до с со у о об про за над под между это этот эта эти если ли не мне нам ему ей их мой моя свои свое своего нужно надо можно нельзя должен должна должны делать сделать произошло происходит ситуация вопрос'.split(' '));const words=s=>norm(s).split(' ').filter(w=>w.length>2&&!stop.has(w));const pref=w=>w.length>5?w.slice(0,5):w;function scoreRule(r,text){const n=norm(text),ws=words(text),hay=norm([r.title,r.verdict,r.note,(r.steps||[]).join(' '),(r.docs||[]).join(' '),(r.refs||[]).join(' '),(r.keywords||[]).join(' '),(r.phrases||[]).join(' ')].join(' '));let score=r.priority||0;(r.phrases||[]).forEach(p=>{if(n.includes(norm(p)))score+=18});(r.keywords||[]).forEach(k=>{const nk=norm(k);if(n.includes(nk))score+=nk.includes(' ')?9:5});let matched=0;for(const w of ws){const p=pref(w);if(hay.includes(w)||hay.split(' ').some(x=>x.startsWith(p)))matched++;}if(ws.length)score+=matched*4+(matched===ws.length?8:0);if(r.id==='document-refusal'&&/(документ|пропуск|удостоверен)/.test(n)&&/(отказ|не хочет|не дает|не даёт|не показывает|не предъяв|не переда|предостав)/.test(n))score+=22;if(r.id==='wrong-pass'&&/(чуж|не свой|другого).*пропуск/.test(n))score+=14;if(r.id==='fire-emergency'&&/(авари|пожар|взрыв|мчс|скорая|спасат)/.test(n))score+=10;if(r.id==='foreign'&&/(иностран|без гражданства)/.test(n))score+=12;if(r.id==='drone'&&/(дрон|бвс|беспилот|квадрокоптер)/.test(n))score+=16;return{score,matched,total:ws.length};}function esc(s){return String(s||'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]))}function renderRule(r,best=false){const refs=[...(r.refs||[]),...(r.amended||[])];return `<article class="card ${best?'best':''}"><div class="meta">${refs.map((x,i)=>`<span class="ref ${i>=(r.refs||[]).length?'amend':''}">${esc(x)}</span>`).join('')}</div><div class="title">${esc(r.title)}</div><div class="answer"><span class="label">ЧТО ГОВОРИТ ИНСТРУКЦИЯ</span>${esc(r.verdict||'')}</div>${r.steps&&r.steps.length?`<div class="block"><span class="label">ЧТО ДЕЛАТЬ</span><ol>${r.steps.map(x=>`<li>${esc(x)}</li>`).join('')}</ol></div>`:''}${r.docs&&r.docs.length?`<div class="block"><span class="label">ЧТО ПРОВЕРИТЬ</span><ul>${r.docs.map(x=>`<li>${esc(x)}</li>`).join('')}</ul></div>`:''}${r.note?`<div class="source"><b>Основание:</b> ${esc(r.note)}</div>`:''}</article>`;}function analyze(){const text=q.value.trim();if(!text){results.innerHTML='';hint.textContent='';return;}const ranked=RULES.map(r=>({r,...scoreRule(r,text)})).sort((a,b)=>b.score-a.score),best=ranked[0];if(!best||best.score<12){results.innerHTML='<div class="notfound"><b>Точного пункта не найдено.</b><br>Уточните, кто прибыл, куда, какие документы есть и что именно происходит.</div>';return;}const selected=ranked.filter((x,i)=>i===0||(x.score>=best.score-9&&x.matched>0)).slice(0,3);results.innerHTML=renderRule(selected[0].r,true)+(selected.length>1?'<div class="secondaryTitle">Также может относиться к ситуации</div>'+selected.slice(1).map(x=>renderRule(x.r,false)).join(''):'');}let timer;q.addEventListener('input',()=>{clearTimeout(timer);timer=setTimeout(analyze,350)});q.addEventListener('keydown',e=>{if(e.key==='Enter'){e.preventDefault();clearTimeout(timer);analyze();}});const SR=window.SpeechRecognition||window.webkitSpeechRecognition;let rec=null,listening=false;if(SR){rec=new SR();rec.lang='ru-RU';rec.interimResults=true;rec.continuous=false;let finalText='';rec.onstart=()=>{listening=true;finalText='';mic.classList.add('on');hint.textContent='Слушаю…';};rec.onresult=e=>{let interim='';for(let i=e.resultIndex;i<e.results.length;i++){const t=e.results[i][0].transcript;if(e.results[i].isFinal)finalText+=t+' ';else interim+=t;}q.value=(finalText+interim).trim();};rec.onerror=e=>{hint.textContent=e.error==='not-allowed'?'Нет разрешения на микрофон':'Не удалось распознать речь';};rec.onend=()=>{listening=false;mic.classList.remove('on');hint.textContent='';if(q.value.trim())analyze();};mic.addEventListener('click',()=>{try{listening?rec.stop():rec.start()}catch(_){}});}else{mic.disabled=true;hint.textContent='Голосовой ввод не поддерживается этим браузером';}if('serviceWorker'in navigator)window.addEventListener('load',()=>navigator.serviceWorker.register('./sw.js').catch(()=>{}));})();
